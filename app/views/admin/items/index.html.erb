<%= render 'admin/homes/admin_header' %>


<!--商品一覧画面-->
<div class="container">
  <div class="row">
    <div class="mt-5 col-sm-2.5">
      <h5 class="bg-light"><strong>商品一覧</strong></h5>
    </div>
  </div>
  
  
  
  <div class="container">
  <div class="row">
   <div class='col-md-10 offset-md-1'>

 <table class='table table-hover table-inverse'>
  <thead>
    <tr>
      <th>商品ID</th>
      <th>商品名</th>
      <th>税抜価格</th>
      <th>ジャンル</th>
      <th>ステータス</th>
      <th colspan="5"></th>
    </tr>
  </thead>
  <tbody>
    
    
    <% @items.each do |item| %>
      <tr>
        <td>
          <%= item.id %>
        </td>
        <td>
          <%= link_to admin_item_path(item.id) do %>
          <%= item.name %>
          <% end %>
        </td>
        <td>
          <%= converting_to_jpy(item.price) %>
        </td>
        <td>
          <%= item.genre.name %>
        </td>
        <td>
          <% if item.is_active == true %>
          <strong class="text-success"><%= "販売中" %></strong>
          <% else %>
          <% item.is_active == false %>
          <strong class="text-secondary"><%= "販売停止中" %></strong>
          <% end %>
        </td>
  
    <% end %>
  </tbody>
 </table>
</div>
</div>
</div>

<%= paginate @items %>